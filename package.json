{
  "name": "wheelwright",
  "version": "0.1.0",
  "description": "Parametric 3D-printable wheel configurator powered by Zoo's Design API — splits any wheel into printer-sized segments that slide together.",
  "license": "MIT",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup:zoo": "node scripts/setup-zoo.mjs",
    "test": "node --test",
    "validate:kcl": "node scripts/validate-kcl.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "three": "^0.160.0"
  }
}
