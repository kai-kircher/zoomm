{
  "name": "wheelwright",
  "version": "0.2.0",
  "description": "Parametric 3D-printable wheel configurator — splits any wheel into printer-sized segments that slide together, and builds them with OpenCascade.",
  "license": "MIT",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup:occ": "node scripts/setup-occ.mjs",
    "test": "node --test",
    "validate": "node scripts/validate-occ.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "three": "^0.160.0"
  }
}
